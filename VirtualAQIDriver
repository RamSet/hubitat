import java.text.DecimalFormat

metadata {
	definition (name: "Virtual AQI Driver", namespace: "RamSet", author: "RamSet") {
	capability "AirQuality"
	capability "Temperature Measurement"
	command "airQualityIndex"
	attribute "airQualityIndex", "number"
	}
}
def installed() {
    log.warn "installed..."
    airQualityIndex(0)
   }

def updated() {
    log.info "updated..."
    log.warn "debug logging is: ${logEnable == true}"
    log.warn "description logging is: ${txtEnable == true}"
}

def parse(String description) {
}

def AQI(String airQualityIndex) {
    def descriptionText = "${device.displayName} AQI is ${String airQualityIndex}"
    if (txtEnable) log.info "${descriptionText}"
    sendEvent(name: "AQI", value: airQualityIndex, descriptionText: descriptionText, isStateChange: true)
}
